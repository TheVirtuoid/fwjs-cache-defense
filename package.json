{
  "name": "fwjs-cache-defense",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npx cypress open",
    "test:ci": "npx cypress run",
    "test:full": "vite build & vite preview & npx cypress open",
    "test:full:ci": "vite build & vite preview & npx cypress run"
  },
  "devDependencies": {
    "cypress": "^12.16.0",
    "cypress-vite": "^1.4.0",
    "vite": "^4.3.9"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  }
}
